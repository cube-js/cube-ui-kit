import { Meta } from '@storybook/addon-docs';
import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BulbFilled, LockFilled } from '@ant-design/icons';

import { baseProps } from './lists/baseProps';
import { Element, Button, Result, Space, Text, Title } from '../index';

<Meta title="Content/Result" component={Result} />

export const Template = ({ children, ...props }) => {
  return <Result {...props}>{children}</Result>;
};

# Result

Used to feed back the results of a series of operational tasks.

## When To Use

Use when important operations need to inform the user to process the results and the feedback is more complicated.

## Examples

### Success

<Canvas>
  <Story
    name="Success"
    args={{
      status: 'success',
      title: 'Successfully Purchased Cloud Server ECS!',
      subtitle:
        'Order number: 2017182818828182881 Cloud server configuration takes 1-5 minutes, please wait.',
      children: (
        <Space>
          <Button type="primary">Go Console</Button>
          <Button type="secondary">Buy Again</Button>
        </Space>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Info

<Canvas>
  <Story
    name="Info"
    args={{
      status: 'info',
      title: 'Your operation has been executed',
      children: <Button type="primary">Go Console</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Warning

<Canvas>
  <Story
    name="Warning"
    args={{
      status: 'warning',
      title: 'There are some problems with your operation',
      children: <Button type="primary">Go Console</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Error

<Canvas>
  <Story
    name="Error"
    args={{
      status: 'error',
      title: 'Submission Failed',
      subtitle:
        'Please check and modify the following information before resubmitting.',
      children: (
        <Space>
          <Button type="primary">Go Console</Button>
          <Button type="secondary">Buy Again</Button>
        </Space>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Custom Icon

<Canvas>
  <Story
    name="Custom Icon"
    args={{
      title: 'Access is denied!',
      subtitle: 'Request access from the administrator.',
      icon: (
        <Element styles={{ color: '#note', fontSize: '10x' }}>
          <LockFilled />
        </Element>
      ),
      children: <Button type="secondary">Request</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Custom Title

<Canvas>
  <Story
    name="Custom Title"
    args={{
      title: (
        <Title level={3} preset="h2">
          Advice
        </Title>
      ),
      subtitle: (
        <Text.Selection>
          Complete your profile to increase search relevancy.
        </Text.Selection>
      ),
      icon: (
        <Element styles={{ color: '#purple', fontSize: '12x' }}>
          <BulbFilled />
        </Element>
      ),
      children: <Button type="primary">Complete Now</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Compact

<Canvas>
  <Story
    name="Compact"
    args={{
      isCompact: true,
      status: 'success',
      title: 'Successfully Purchased Cloud Server ECS!',
      subtitle:
        'Order number: 2017182818828182881 Cloud server configuration takes 1-5 minutes, please wait.',
      children: (
        <Space>
          <Button type="primary">Go Console</Button>
          <Button type="secondary">Buy Again</Button>
        </Space>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## API

<ArgsTable of={Result} exclude={['isDisabled', 'theme', ...baseProps]} />

## Differences from Ant Design

| There are differences | Ant Design property | Cube UIKit property |
| :-------------------: | :-----------------: | :-----------------: |
|          No           |      `status`       |      `status`       |
|          No           |       `title`       |       `title`       |
|          Yes          |     `subTitle`      |     `subtitle`      |
|          No           |       `icon`        |       `icon`        |
|          Yes          |       `extra`       |     `children`      |
