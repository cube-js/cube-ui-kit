import { Meta } from '@storybook/addon-docs';
import { Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Base } from '../index';

<Meta title="UIKit/Base" component={Base}/>

# Base element

The `Base` component is a basic brick of the UI system. It has some useful shorthands for rapid UI development.

<ArgsTable of={Base}/>

The most of the components share these base properties.
