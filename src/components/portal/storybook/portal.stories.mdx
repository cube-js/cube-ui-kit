import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs';
import { Portal } from '../Portal';
import { CustomRoot, Default, Disabled, Playground } from './portal.stories';
import { Root } from '../../Root';

<Meta
  title="Helpers/Portal"
  component={Portal}
  parameters={{ layout: 'centered' }}
/>

# Portal

<Description />

## Examples

### Default

In this example you can see that Portal's content renders outside of

<Canvas>
  <Story story={Default} name="Default Portal" />
</Canvas>

### Disabled Portal

When `<Portal />` is disabled, it behaves as usual element:

<Canvas>
  <Story story={Disabled} name="Disabled Portal" />
</Canvas>

### Custom root

By setting `root` prop, you can manage, where this portal should render.

<Canvas>
  <Story story={CustomRoot} name="Custom Root" />
</Canvas>

### Portal order

Here you can see the order of portals

<Canvas>
  <Root>
    <Portal>
      <div>Portal 1</div>
      <Portal>
        <div>Portal 3</div>
      </Portal>
      <Portal>
        <div>Portal 4</div>
      </Portal>
    </Portal>
    <Portal>
      <div>Portal 2</div>
    </Portal>
  </Root>
</Canvas>

## Playground

<Canvas>
  <Story story={Playground} name="Args" />
</Canvas>

<ArgsTable story="Args" />
