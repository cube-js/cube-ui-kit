import { IconPlaystationTriangle } from '@tabler/icons-react';

import { wrapIcon } from './wrap-icon';

export const PlayCircleIcon = wrapIcon(
  'PlayCircleIcon',
  <IconPlaystationTriangle style={{ rotate: '90deg' }} />,
);
