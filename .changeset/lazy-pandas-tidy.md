---
'@cube-dev/ui-kit': patch
---

Update `@tenphi/tasty` to 2.10.0 and `@tenphi/eslint-plugin-tasty` to 0.11.1, and reformat the styles the new lint rules cover. No public API or rendering change — every rewrite was verified to emit identical CSS.
