---
'@cube-dev/ui-kit': minor
---

Add `s2`, `s3`, and `s4` monospace typography presets for code and snippet text (sized to match `t2`–`t4`, using `--font-mono`). `PrismCode` and `CopySnippet` now use `s3` by default.
