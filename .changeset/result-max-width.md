---
'@cube-dev/ui-kit': patch
---

`Result` now sets a default `width: max 80ch`, capping the component at a comfortable reading width (~80 characters) on wide screens. Pass a `width` prop to override the cap.
