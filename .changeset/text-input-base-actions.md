---
"@cube-dev/ui-kit": minor
---

`TextInputBase` takes an `actions` slot for controls that always sit last in the suffix, after the validation and loading icons and after `suffix` in either position, such as a popover trigger.
