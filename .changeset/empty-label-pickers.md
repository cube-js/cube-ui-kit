---
'@cube-dev/ui-kit': minor
---

Add `emptyLabel` support to ComboBox, Picker, and Select. When provided, the popover stays open and shows the message for an empty (or filtered-empty) list; when omitted, the popover stays closed.
