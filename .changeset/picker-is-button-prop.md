---
'@cube-dev/ui-kit': minor
---

Add `isButton` prop support to `Picker` and `FilterPicker` components. The prop is now properly passed to the trigger `ItemButton` component, allowing control over button styling. Defaults to `false` to maintain existing behavior.

